// Generated by CoffeeScript 1.6.2
(function() {
  var arr, i, key, obj, siteName, value, _i, _j, _k, _l, _len, _len1, _len2, _len3;

  arr = ["Nettuts+", "Aetuts+", "Phototuts+", "Psdtuts+", "Cgtuts+", "MobileTuts+"];

  obj = {
    name: "Nettuts+",
    topic: "Development",
    editor: "Jeffrey Way"
  };

  /*
  in javascript
  for(var i =0; i < arr.length; i++) {
  	console.log(arr[i]);
  }
  */


  for (i = _i = 0, _len = arr.length; _i < _len; i = ++_i) {
    siteName = arr[i];
    if (siteName.indexOf("P") === 0) {
      console.log("" + i + ": " + siteName);
    }
  }

  for (i = _j = 0, _len1 = arr.length; _j < _len1; i = _j += 2) {
    siteName = arr[i];
    console.log("" + i + ": " + siteName);
  }

  for (i = _k = 0, _len2 = arr.length; _k < _len2; i = ++_k) {
    siteName = arr[i];
    if (i % 2 === 0) {
      console.log("" + i + ": " + siteName);
    }
  }

  console.log("##################################");

  for (i = _l = 0, _len3 = arr.length; _l < _len3; i = ++_l) {
    siteName = arr[i];
    console.log("" + i + ": " + siteName);
  }

  console.log((function() {
    var _len4, _m, _results;

    _results = [];
    for (_m = 0, _len4 = arr.length; _m < _len4; _m++) {
      siteName = arr[_m];
      _results.push(siteName.length);
    }
    return _results;
  })());

  /* loop objects
  */


  for (key in obj) {
    value = obj[key];
    console.log("" + key + ": " + value);
  }

}).call(this);
